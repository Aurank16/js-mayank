"use strict"// it is syntex which shows or treat js code as newer version.

// alert(3+3) // we are using node.js not browser'

console.log(3+3);console.log("hello") // this is ryt bt not in not standerd way.

let name ="mayank"
let age = 23
let isLoggedIn= false // there all are ex of datatype

// some datatype are:-
// number => 2^53(range) 
// bigint=>(if thr number is too large then it get use bt normally not)
// string=> ""
// boolean=> true/false
// null=> a standalone value
// undefine=> standalone bt it is defined bt value is not assigned
// symbol=> to find uniqness

console.log(typeof null)/* typeof used to define type of datatype 
use in console.log */
// null is an object

console.log(typeof undefined)// o/p:- undefine bt in null it is obj.




 
